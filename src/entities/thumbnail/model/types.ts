export type PlayerDraft = {
  thumbnailTime: number | null
  thumbnailUrl?: string
  start: number
  end: number
}

export type PlayerItem = {
  id: string
  thumbnailUrl: string
  thumbnailTime: number
  start: number
  end: number
}

